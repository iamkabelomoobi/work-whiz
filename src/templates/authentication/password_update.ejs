<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Password Changed</title>
</head>
<body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px;">
  <div style="background-color: #f4f4f4; padding: 20px; border-radius: 5px;">
    <h1 style="color: #2c3e50; margin-bottom: 20px;">Your Password Was Changed</h1>
    
    <p>Hello,</p>
    
    <p>This email confirms that the password for your <%= appName %> account (<%= email %>) has been successfully changed.</p>
    
    <% if (typeof device !== 'undefined' && device) { %>
    <div style="background-color: #fff; padding: 15px; border-radius: 5px; margin: 20px 0;">
      <h3 style="color: #2c3e50; margin-top: 0;">Change Details:</h3>
      <p style="margin: 5px 0;"><strong>Time:</strong> <%= new Date(device.timestamp).toLocaleString() %></p>
      <% if (device.ip) { %>
      <p style="margin: 5px 0;"><strong>IP Address:</strong> <%= device.ip %></p>
      <% } %>
      <% if (device.location && (device.location.city || device.location.country)) { %>
      <p style="margin: 5px 0;"><strong>Location:</strong> <%= device.location.city || '' %><%= device.location.city && device.location.country ? ', ' : '' %><%= device.location.country || '' %></p>
      <% } %>
    </div>
    <% } %>
    
    <p style="color: #e74c3c; font-weight: bold;">If you did not make this change, please contact support immediately.</p>
    
    <% if (typeof supportLink !== 'undefined' && supportLink) { %>
    <div style="text-align: center; margin: 30px 0;">
      <a href="<%= supportLink %>" style="background-color: #e74c3c; color: white; padding: 12px 30px; text-decoration: none; border-radius: 5px; display: inline-block;">Contact Support</a>
    </div>
    <% } %>
    
    <hr style="border: none; border-top: 1px solid #ddd; margin: 30px 0;">
    
    <p style="color: #7f8c8d; font-size: 12px;">
      This is an automated security notification from <%= appName %>. Please do not reply to this email.
    </p>
  </div>
</body>
</html>